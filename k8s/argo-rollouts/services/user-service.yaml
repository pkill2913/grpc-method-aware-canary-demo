apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: app-demo
spec:
  # selector:
  #   app: user-service
  ports:
  - name: grpc
    appProtocol: kubernetes.io/h2c
    port: 50051
    targetPort: grpc