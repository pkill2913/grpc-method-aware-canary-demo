apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: frontend-route
  namespace: app-demo
spec:
  parentRefs:
  - name: app-gateway
    namespace: app-demo
  rules:
  - backendRefs:
    - name: frontend
      port: 8080
